# =============================================================================
# CAPM testbed - FULL environment lock (reproduces the .venv exactly)
# =============================================================================
# Use this when you want EVERYTHING (so every experiment runs, incl. E5.4 which
# needs agentdojo). Captured from the working .venv with `pip freeze`.
# Python: 3.12.3
#
# HOW TO USE (any phase, e.g. phase1/):
#   cd <copy>/files/capm-testbed
#   python3 -m venv .venv
#   .venv/bin/pip install -U pip
#   .venv/bin/pip install -r requirements-full.txt
#   # then run from the venv:
#   .venv/bin/python -m experiments.run_flow
#
# MINIMAL alternative (core testbed only, no agentdojo / E5.4):
#   pip install cryptography>=42  pytest      # everything else is stdlib
#   # (the standalone testbed + S0-S3 + E1-E4(REST Gemini) + E6-E9 run on this)
#
# NOT pip-installable (install separately, sudo-free, via scripts/setup_realism.sh):
#   * ProVerif 2.05 CLI  -> E2.1 formal proof   (opam + OCaml build -> ~/.local/bin/proverif)
#
# OPTIONAL model SDKs: this lock already includes anthropic / google-genai /
# openai (pulled by agentdojo). The testbed's Gemini backend uses plain REST
# (urllib), so these SDKs are NOT required for the --llm experiments - they are
# here only because agentdojo depends on them.
# =============================================================================

# ---- direct deps the testbed actually uses --------------------------------
cryptography==49.0.0          # Ed25519 signing/verification (core)
agentdojo==0.1.35             # real cross-org benchmark suites (E5.4)

# ---- full transitive lock (exact versions, for byte-for-byte reproduction) -
annotated-doc==0.0.4
annotated-types==0.7.0
anthropic==0.109.1
anyio==4.13.0
cachebox==5.2.3
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
cohere==7.0.4
deepdiff==9.1.0
distro==1.9.0
dnspython==2.8.0
docstring_parser==0.18.0
email-validator==2.3.0
fastavro==1.12.2
filelock==3.29.3
fsspec==2026.4.0
google-auth==2.54.0
google-genai==2.8.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.19.0
idna==3.18
jiter==0.15.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain==1.3.9
langchain-core==1.4.7
langchain-protocol==0.0.17
langgraph==1.2.5
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.8.15
markdown-it-py==4.2.0
mdurl==0.1.2
openai==2.41.1
orderly-set==5.5.0
orjson==3.11.9
ormsgpack==1.12.2
packaging==26.2
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
rich==15.0.0
shellingham==1.5.4
sniffio==1.3.1
tenacity==9.1.4
tokenizers==0.23.1
tqdm==4.68.2
typer==0.25.1
types-requests==2.33.0.20260518
typing_extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.7.0
uuid_utils==0.16.0
websockets==15.0.1
xxhash==3.7.0
zstandard==0.25.0

"""Synthetic agent-ecosystem models for the Goal-2 attack experiments.

The residual that B1 localised (origin-class capture) is exploited at the
*ecosystem* level: an attacker chooses *which* authoritative origin to capture.
:mod:`capm.ecosystem.graph` generates populations of origins whose **warrant
ceiling** (what they're worth to capture) and **integrity strength** (what they
cost to capture) are two *independent* axes whose coupling is a tunable knob —
the very coupling the WGOT attack (B3) and cartography (B4) study.
"""
